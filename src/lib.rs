#![allow(warnings)]
#![no_std]
mod bitfield;
mod bitmap;
mod errors;
mod math;
mod types;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
